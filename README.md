# Credit Scoring Model - PD Model Pipeline

Автоматизированный пайплайн для разработки, тестирования, развёртывания и мониторинга модели машинного обучения для предсказания вероятности дефолта клиента.

## 📋 Описание проекта

Проект реализует end-to-end ML-пайплайн для скоринговой модели предсказания дефолта (Probability of Default Model) на основе датасета Default of Credit Card Clients Dataset с UCI Machine Learning Repository.

**Домен:** Финансы / Кредитный скоринг

**Целевая задача:** Бинарная классификация - предсказание вероятности дефолта клиента в следующем месяце

## 🏗️ Архитектура проекта

```
credit-scoring-model/
├── data/                           # Данные (версионируются через DVC)
│   ├── raw/                       # Исходные данные
│   ├── processed/                 # Обработанные данные
│   └── expectations/              # Great Expectations suite
├── models/                        # Сохраненные модели (версионируются через DVC)
├── notebooks/                     # Jupyter notebooks для EDA
├── src/                          # Исходный код
│   ├── data/                     # Скрипты для работы с данными
│   │   ├── make_dataset.py      # Загрузка и первичная обработка
│   │   └── validation.py        # Валидация данных с Great Expectations
│   ├── features/                # Feature Engineering
│   │   └── build_features.py    # Создание признаков
│   ├── models/                  # Обучение и предсказание
│   │   ├── train.py            # Обучение модели
│   │   ├── predict.py          # Inference
│   │   └── pipeline.py         # Sklearn Pipeline
│   └── api/                     # FastAPI приложение
│       └── app.py              # REST API endpoint
├── tests/                       # Unit-тесты
│   ├── test_data.py
│   ├── test_features.py
│   └── test_models.py
├── .github/workflows/           # GitHub Actions CI/CD
│   └── ci-cd.yml
├── Dockerfile                   # Docker контейнер
├── requirements.txt             # Python зависимости
├── dvc.yaml                     # DVC pipeline конфигурация
├── pyproject.toml              # Конфигурация инструментов
└── README.md
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.9+
- Git
- Docker (опционально)
- DVC

### Установка

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd credit-scoring-model
```

2. **Создание виртуального окружения:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

4. **Инициализация DVC:**
```bash
dvc init
dvc pull  # Получение данных и моделей
```

### Запуск пайплайна

**Полный DVC пайплайн:**
```bash
dvc repro
```

**Отдельные этапы:**
```bash
# Подготовка данных
python src/data/make_dataset.py

# Feature Engineering
python src/features/build_features.py

# Валидация данных
python src/data/validation.py

# Обучение модели
python src/models/train.py
```

### Запуск MLflow UI

```bash
mlflow ui --port 5000
```

Откройте браузер по адресу `http://localhost:5000` для просмотра экспериментов.

## 🧪 Тестирование

**Запуск всех тестов:**
```bash
pytest tests/ -v
```

**Запуск с покрытием кода:**
```bash
pytest tests/ --cov=src --cov-report=html
```

**Проверка качества кода:**
```bash
# Форматирование
black src/ tests/

# Линтинг
flake8 src/ tests/
```

## 🐳 Docker

**Сборка образа:**
```bash
docker build -t credit-scoring-api .
```

**Запуск контейнера:**
```bash
docker run -p 8000:8000 credit-scoring-api
```

**API будет доступен по адресу:** `http://localhost:8000`

**Документация API:** `http://localhost:8000/docs`

## 📊 Использование API

### Пример запроса для предсказания:

```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "LIMIT_BAL": 20000.0,
    "SEX": 2,
    "EDUCATION": 2,
    "MARRIAGE": 1,
    "AGE": 24,
    "PAY_0": 2,
    "BILL_AMT1": 3913.0,
    "PAY_AMT1": 0.0
  }'
```

### Пример ответа:

```json
{
  "default_prediction": 1,
  "default_probability": 0.73
}
```

## 📈 Метрики модели

Основные метрики качества модели:
- **ROC-AUC:** Площадь под ROC-кривой
- **Precision:** Точность предсказания дефолта
- **Recall:** Полнота определения дефолта
- **F1-Score:** Гармоническое среднее precision и recall

Все метрики логируются в MLflow и отображаются в UI.

## 🔍 Мониторинг

Скрипт для проверки дрифта данных:

```bash
python src/monitoring/drift_detection.py
```

Рассчитывает Population Stability Index (PSI) для обнаружения изменений в распределении данных.

## 📦 Версионирование данных и моделей

Проект использует DVC для версионирования:
- Исходных данных (`data/raw/`)
- Обработанных данных (`data/processed/`)
- Обученных моделей (`models/`)

**Сохранение изменений:**
```bash
dvc add data/raw/UCI_Credit_Card.csv
dvc add models/credit_default_model.pkl
dvc push
git add data/.gitignore models/.gitignore *.dvc
git commit -m "Update data and models"
```

## 🔄 CI/CD Pipeline

GitHub Actions автоматически выполняет:
1. ✅ Линтинг кода (flake8)
2. ✅ Форматирование (black)
3. ✅ Unit-тесты (pytest)
4. ✅ Валидацию данных (Great Expectations)
5. ✅ Сборку Docker образа

## 📚 Технологический стек

- **ML:** scikit-learn, pandas, numpy
- **Tracking:** MLflow
- **Version Control:** Git, DVC
- **Data Validation:** Great Expectations
- **API:** FastAPI, Pydantic
- **Testing:** pytest
- **CI/CD:** GitHub Actions
- **Containerization:** Docker
- **Code Quality:** black, flake8

## 👨‍💻 Разработка

### Создание нового эксперимента:

1. Отредактируйте параметры в `src/models/train.py`
2. Запустите обучение
3. Проверьте результаты в MLflow UI
4. Сравните метрики разных экспериментов

### Добавление новых признаков:

1. Добавьте логику в `src/features/build_features.py`
2. Обновите валидацию в `src/data/validation.py`
3. Запустите тесты
4. Переобучите модель


## 📧 Контакты

При возникновении вопросов создайте issue в репозитории.

---


**Датасет:** [UCI Credit Card Default Dataset]([https://archive.ics.uci.edu/ml/datasets/default+of+credit+card+clients](https://www.kaggle.com/datasets/uciml/default-of-credit-card-clients-dataset?spm=a2ty_o01.29997173.0.0.532fc921UIOjfa](https://www.kaggle.com/datasets/uciml/default-of-credit-card-clients-dataset?spm=a2ty_o01.29997173.0.0.532fc921UIOjfa))
