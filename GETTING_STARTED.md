# Пошаговое руководство по запуску проекта

## 🎯 Быстрый старт (для новичков)

Следуйте этим инструкциям шаг за шагом, чтобы запустить проект.

### Шаг 1: Проверка предустановленного ПО

Убедитесь, что у вас установлено:

```bash
# Проверка Python (должна быть версия 3.9 или выше)
python --version
# или
python3 --version

# Проверка Git
git --version

# Проверка pip
pip --version
```

Если что-то не установлено, установите:
- Python: https://www.python.org/downloads/
- Git: https://git-scm.com/downloads

### Шаг 2: Клонирование репозитория (или использование локальных файлов)

Если у вас уже есть файлы проекта локально, перейдите в директорию проекта:

```bash
cd путь/к/credit-scoring-model
```

Или клонируйте из Git (если загрузили в репозиторий):

```bash
git clone <URL-вашего-репозитория>
cd credit-scoring-model
```

### Шаг 3: Создание виртуального окружения

**Для Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Для Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

После активации вы должны увидеть `(venv)` перед командной строкой.

### Шаг 4: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Это может занять несколько минут.

### Шаг 5: Инициализация DVC

```bash
dvc init
```

### Шаг 6: Запуск полного пайплайна

**Вариант A: Автоматический (рекомендуется для начинающих)**

**Linux/Mac:**
```bash
chmod +x run_pipeline.sh
./run_pipeline.sh
```

**Windows:**
Выполните команды по очереди:
```cmd
python src/data/make_dataset.py
python src/features/build_features.py
python src/data/validation.py
python src/models/train.py --model gradient_boosting
python src/monitoring/drift_detection.py --mode drift
```

**Вариант B: С использованием DVC**

```bash
dvc repro
```

### Шаг 7: Просмотр результатов в MLflow

Откройте новый терминал (не закрывая предыдущий) и запустите:

```bash
cd путь/к/credit-scoring-model
source venv/bin/activate  # для Linux/Mac
# или venv\Scripts\activate для Windows

mlflow ui
```

Откройте браузер и перейдите по адресу: http://localhost:5000

Здесь вы увидите все эксперименты и метрики моделей.

### Шаг 8: Запуск API

**Вариант A: Локальный запуск**

```bash
uvicorn src.api.app:app --reload --host 0.0.0.0 --port 8000
```

API будет доступен по адресу: http://localhost:8000

Документация API: http://localhost:8000/docs

**Вариант B: Запуск в Docker**

Если у вас установлен Docker:

```bash
# Сборка образа
docker build -t credit-scoring-api .

# Запуск контейнера
docker run -d -p 8000:8000 credit-scoring-api

# Проверка что контейнер работает
docker ps
```

### Шаг 9: Тестирование API

Откройте новый терминал и выполните:

```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "LIMIT_BAL": 20000.0,
    "SEX": 2,
    "EDUCATION": 2,
    "MARRIAGE": 1,
    "AGE": 24,
    "PAY_0": 2,
    "BILL_AMT1": 3913.0,
    "PAY_AMT1": 0.0
  }'
```

Или откройте браузер и перейдите по адресу http://localhost:8000/docs для интерактивной документации.

### Шаг 10: Запуск тестов

```bash
# Все тесты
pytest tests/ -v

# С покрытием кода
pytest tests/ --cov=src --cov-report=html

# Открыть отчет о покрытии (создается папка htmlcov/)
# Linux/Mac:
open htmlcov/index.html
# Windows:
start htmlcov/index.html
```

### Шаг 11: Проверка качества кода

```bash
# Форматирование кода
black src/ tests/

# Линтинг
flake8 src/ tests/
```

---

## 🔄 Проведение экспериментов

Чтобы провести новый эксперимент с другими параметрами:

### 1. Эксперимент с Logistic Regression:

```bash
python src/models/train.py --model logistic_regression
```

### 2. Эксперимент с Grid Search:

```bash
python src/models/train.py --model gradient_boosting --grid-search
```

### 3. Просмотр результатов:

Откройте MLflow UI (если еще не открыт):
```bash
mlflow ui
```

Перейдите на http://localhost:5000 и сравните эксперименты.

---

## 📊 Работа с данными

### Повторная подготовка данных:

```bash
python src/data/make_dataset.py
```

### Валидация данных:

```bash
python src/data/validation.py
```

### Feature Engineering:

```bash
python src/features/build_features.py
```

---

## 🔍 Мониторинг дрифта

### Проверка дрифта данных (PSI):

```bash
python src/monitoring/drift_detection.py --mode drift
```

### Имитация отправки данных на API:

Убедитесь, что API запущен, затем:

```bash
python src/monitoring/drift_detection.py --mode api --api-url http://localhost:8000
```

---

## 🐛 Решение проблем

### Проблема: ModuleNotFoundError

**Решение:** Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
source venv/bin/activate  # или venv\Scripts\activate для Windows
pip install -r requirements.txt
```

### Проблема: Порт 8000 уже занят

**Решение:** Используйте другой порт:
```bash
uvicorn src.api.app:app --reload --port 8001
```

### Проблема: Модель не найдена при запуске API

**Решение:** Сначала обучите модель:
```bash
python src/models/train.py --model gradient_boosting
```

### Проблема: DVC ошибки

**Решение:** Переинициализируйте DVC:
```bash
rm -rf .dvc
dvc init
```

---

## 📝 Структура проекта

```
credit-scoring-model/
├── data/                    # Данные (управляется DVC)
│   ├── raw/                # Исходные данные
│   └── processed/          # Обработанные данные
├── models/                 # Обученные модели
├── notebooks/              # Jupyter notebooks для анализа
├── src/                    # Исходный код
│   ├── data/              # Обработка данных
│   ├── features/          # Feature engineering
│   ├── models/            # Обучение моделей
│   ├── api/               # FastAPI приложение
│   └── monitoring/        # Мониторинг
├── tests/                  # Тесты
├── .github/workflows/      # CI/CD
├── Dockerfile             # Docker конфигурация
├── requirements.txt       # Python зависимости
├── dvc.yaml              # DVC pipeline
└── README.md             # Документация
```

---

## 🎓 Дополнительные материалы

- **MLflow Documentation:** https://mlflow.org/docs/latest/index.html
- **DVC Documentation:** https://dvc.org/doc
- **FastAPI Documentation:** https://fastapi.tiangolo.com/
- **Great Expectations:** https://docs.greatexpectations.io/

---

## 💡 Советы для презентации проекта

1. **Покажите MLflow UI** - продемонстрируйте эксперименты и сравнение метрик
2. **Запустите API** - сделайте несколько предсказаний через Swagger UI
3. **Покажите тесты** - запустите pytest с выводом покрытия
4. **Продемонстрируйте CI/CD** - покажите GitHub Actions workflow
5. **Покажите мониторинг** - запустите детекцию дрифта

---

## ✅ Чеклист перед презентацией

- [ ] Все зависимости установлены
- [ ] Данные подготовлены (train.csv и test.csv существуют)
- [ ] Модель обучена и сохранена
- [ ] Тесты проходят успешно
- [ ] API запускается и отвечает
- [ ] MLflow UI показывает эксперименты
- [ ] Docker образ собирается
- [ ] GitHub Actions настроен

Удачи с проектом! 🚀
